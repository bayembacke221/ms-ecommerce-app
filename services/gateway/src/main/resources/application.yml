spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: "http://localhost:9099/realms/ms-ecommerce"
  config:
    import: optional:configserver:http://localhost:8888
  application:
    name: gateway-service